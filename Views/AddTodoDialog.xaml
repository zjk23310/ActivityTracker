<Window x:Class="ActivityTracker.Views.AddTodoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加待办"
        Width="450"
        Height="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="18">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0"
                   Text="标题"
                   Margin="0,0,0,6"/>

        <TextBox Grid.Row="1"
                 x:Name="TitleBox"
                 FontSize="14"
                 MaxLength="200"/>

        <!-- 备注 -->
        <TextBlock Grid.Row="2"
                   Text="备注"
                   Margin="0,14,0,6"/>

        <TextBox Grid.Row="3"
                 x:Name="NoteBox"
                 FontSize="14"
                 MinHeight="70"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 MaxLength="1000"/>

        <!-- 优先级 + 分类 -->
        <Grid Grid.Row="4"
              Margin="0,14,0,6">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="优先级"/>
            <TextBlock Grid.Column="1"
                       Text="分类"
                       Margin="14,0,0,0"/>

        </Grid>

        <Grid Grid.Row="5">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- SelectedIndex 直接对应 0=普通 1=重要 2=紧急 -->
            <ComboBox Grid.Column="0"
                      x:Name="PriorityBox"
                      SelectedIndex="0">
                <ComboBoxItem Content="普通"/>
                <ComboBoxItem Content="重要"/>
                <ComboBoxItem Content="紧急"/>
            </ComboBox>

            <TextBox Grid.Column="1"
                     x:Name="CategoryBox"
                     Margin="14,0,0,0"
                     MaxLength="50"/>

        </Grid>

        <!-- 截止日期 -->
        <TextBlock Grid.Row="6"
                   Text="截止日期"
                   Margin="0,14,0,6"/>

        <Grid Grid.Row="7">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DatePicker Grid.Column="0"
                        x:Name="DueDatePicker"
                        Width="150"
                        VerticalAlignment="Center"/>

            <CheckBox Grid.Column="1"
                      x:Name="ExactTimeCheck"
                      Content="具体到时刻"
                      VerticalAlignment="Center"
                      Margin="16,0,0,0"
                      Checked="ExactTimeCheck_Changed"
                      Unchecked="ExactTimeCheck_Changed"/>

            <TextBox Grid.Column="2"
                     x:Name="TimeBox"
                     Width="70"
                     Margin="12,0,0,0"
                     HorizontalAlignment="Left"
                     IsEnabled="False"
                     ToolTip="24 小时制，例如 14:30"/>

        </Grid>

        <!-- 按钮 -->
        <StackPanel Grid.Row="8"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">

            <Button Content="确定"
                    Width="80"
                    Click="Ok_Click"/>

            <Button Content="取消"
                    Width="80"
                    Margin="10,0,0,0"
                    Click="Cancel_Click"/>

        </StackPanel>

    </Grid>

</Window>
